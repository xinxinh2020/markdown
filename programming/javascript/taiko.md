



## 常用命令

```javascript
openBrowser() // 打开浏览器
goto('https://uat.starlight-dev.nscc-gz.cn') 
focus(textBox({name: 'username'})) // 输入框
write('hello')  // 输入数据
click('hello')  // 点击文本hello
click($('.el-icon-close')); // 通过css样式获取元素
click($('#el-icon-close')); // 通过id获取元素

```

